{
    "name": "voxify-bot",
    "version": "1.0.0",
    "description": "Revolutionize your Discord server with seamless voice channel management. Multilingual support, blazing speed, and advanced features for an unparalleled user experience.",
    "main": "index.js",
    "scripts": {
        "devStart": "NODE_ENV=development tsx watch src/index.ts",
        "dev": "NODE_ENV=development tsx src/index.ts",
        "buildClean": "npm run build:clean && npm run build",
        "build": "npm run build:tsc && npm run build:copy-files && npm run build:install-dependencies && npm run build:copy-env",
        "build:min": "npm run build && npm run build:minify",
        "build:clean": "rm -rf dist",
        "build:tsc": "tsc",
        "build:copy-files": "cp package.json ./dist/package.json && cp package-lock.json ./dist/package-lock.json && cp -n .env.example ./dist/.env || true",
        "build:install-dependencies": "npm --prefix ./dist ci || true",
        "build:copy-env": "cp -n .env.example ./dist/.env || true",
        "build:minify": "node uglify",
        "start": "NODE_ENV=production node .",
        "prettier": "npx prettier --write .",
        "licenseCheck": "license-check-and-add check -f ./licenseConfig.json",
        "licenseFormat": "license-check-and-add add -f ./licenseConfig.json",
        "test": "echo \"Error: no test specified\" && exit 0"
    },
    "repository": {
        "type": "git",
        "url": "git+ssh://git@github.com/VoxifyDevelopment/VoxifyBot.git"
    },
    "keywords": [
        "discord",
        "voice",
        "bot",
        "discord-bot",
        "temp-voice",
        "voicebot"
    ],
    "author": "VoxifyDevelopment <contact@voxify.dev>",
    "license": "UNLICENSED",
    "bugs": {
        "url": "https://github.com/VoxifyDevelopment/VoxifyBot/issues"
    },
    "homepage": "https://github.com/VoxifyDevelopment/VoxifyBot#readme",
    "dependencies": {
        "discord.js": "^14.14.1",
        "dotenv": "^16.3.1",
        "redis": "^4.6.12"
    },
    "devDependencies": {
        "@types/node": "^20.10.7",
        "find": "^0.3.0",
        "jsonminify": "^0.4.2",
        "license-check-and-add": "^4.0.5",
        "prettier": "^3.1.1",
        "terser": "^5.26.0",
        "tsx": "^4.7.0",
        "typescript": "^5.3.3"
    }
}
